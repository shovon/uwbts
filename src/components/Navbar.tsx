import React = require('react');

const Navbar = () => (
  <nav className="navbar navbar-default navbar-top">
    <div className="container-fluid">
      <div className="navbar-brand" href="#">Brand</div>
    </div>
  </nav>
)

export default Navbar;
